*fibonacci series solution without using any optimization
Time Complexity = o( 2 ^ n )
space complexity = O (n) == for recursion stack
*

  int fib(int n) {
        
        if( n <= 1){ return n;}
        
        return fib( n-1 )+ fib( n- 2);
        
    }
